
import SwiftUI

enum AppTheme {
    static let primary = Color.pink
    static let background = Color(red: 0.1, green: 0.05, blue: 0.08) // marrón oscuro
    static let cardBackground = Color(red: 0.25, green: 0.18, blue: 0.22) // violeta suave
}
